Running VERIFY stage checks...
  [WARN] code_signal_obligations_reviewed: 49 code signal obligation ownership finding(s) (234ms)

  VERIFY Stage Checks
  ────────────────────────
  [WARN] code_signal_obligations_reviewed 49 code signal obligation ownership finding(s)
       SYS-REQ-002 -> lsp-daemon/src/daemon.rs has signal "channel_communication" (creates channel via tokio::sync::mpsc::channel; inferred tags: concurrent, ipc) but no covering requirement closes obligation(s): external_call_concurrency_bounded, external_call_failure_observable, external_call_timeout_bounded, upstream_budget_coordinated. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-002 -> lsp-daemon/src/daemon.rs has signal "concurrency_spawn" (spawns task via tokio::spawn; inferred tags: concurrent) but no covering requirement closes obligation(s): concurrent. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-002 -> lsp-daemon/src/daemon.rs has signal "error_discarded" (discards error via .ok() on Result; inferred tags: error_propagation) but no covering requirement closes obligation(s): errors_propagated. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-002 -> lsp-daemon/src/daemon.rs has signal "filesystem_dependency" (uses filesystem via std::fs::read_to_string; inferred tags: fs_io) but no covering requirement closes obligation(s): external_call_concurrency_bounded, external_call_failure_observable, external_call_timeout_bounded, upstream_budget_coordinated. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-002 -> lsp-daemon/src/daemon.rs has signal "lossy_string_conversion" (calls .to_string_lossy() (drops invalid bytes); inferred tags: binary_safe_io) but no covering requirement closes obligation(s): binary_data_preserved. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-002 -> lsp-daemon/src/daemon.rs has signal "panic_risk" (calls .expect() (panics on Err/None); inferred tags: panic_safety) but no covering requirement closes obligation(s): panic_free_input_handling. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-002 -> lsp-daemon/src/daemon.rs has signal "unsafe_block" (uses unsafe block; inferred tags: unsafe_audited) but no covering requirement closes obligation(s): unsafe_block_documented. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-002 -> lsp-daemon/src/pool.rs has signal "concurrency_spawn" (spawns task via tokio::spawn; inferred tags: concurrent) but no covering requirement closes obligation(s): concurrent. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-002 -> lsp-daemon/src/server_manager.rs has signal "channel_communication" (creates channel via tokio::sync::broadcast::channel; inferred tags: concurrent, ipc) but no covering requirement closes obligation(s): external_call_concurrency_bounded, external_call_failure_observable, external_call_timeout_bounded, upstream_budget_coordinated. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-002 -> lsp-daemon/src/server_manager.rs has signal "concurrency_spawn" (spawns task via tokio::spawn; inferred tags: concurrent) but no covering requirement closes obligation(s): concurrent. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-002 -> lsp-daemon/src/server_manager.rs has signal "error_discarded" (discards error via .ok() on Result; inferred tags: error_propagation) but no covering requirement closes obligation(s): errors_propagated. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-002 -> lsp-daemon/src/server_manager.rs has signal "unsafe_block" (uses unsafe block; inferred tags: unsafe_audited) but no covering requirement closes obligation(s): unsafe_block_documented. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-009 -> src/bert_reranker.rs has signal "concurrency_spawn" (spawns task via std::thread::spawn; inferred tags: concurrent) but no covering requirement closes obligation(s): concurrent. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-009 -> src/bert_reranker.rs has signal "error_discarded" (discards result via let _ = <call>; inferred tags: error_propagation) but no covering requirement closes obligation(s): errors_propagated. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-009 -> src/bert_reranker.rs has signal "filesystem_dependency" (uses filesystem via std::fs::read_to_string; inferred tags: fs_io) but no covering requirement closes obligation(s): external_call_concurrency_bounded, external_call_failure_observable, external_call_timeout_bounded, upstream_budget_coordinated. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-009 -> src/bert_reranker.rs has signal "panic_risk" (uses slice indexing (panics on out-of-bounds); inferred tags: panic_safety) but no covering requirement closes obligation(s): panic_free_input_handling. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-009 -> src/bert_reranker.rs has signal "unsafe_block" (uses unsafe block; inferred tags: unsafe_audited) but no covering requirement closes obligation(s): unsafe_block_documented. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-008 -> src/config.rs has signal "filesystem_dependency" (uses filesystem via std::fs::create_dir_all; inferred tags: fs_io) but no covering requirement closes obligation(s): external_call_concurrency_bounded, external_call_failure_observable, external_call_timeout_bounded, upstream_budget_coordinated. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-008 -> src/config.rs has signal "panic_risk" (uses slice indexing (panics on out-of-bounds); inferred tags: panic_safety) but no covering requirement closes obligation(s): panic_free_input_handling. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-008 -> src/config.rs has signal "toctou_pair" (checks path via .exists then uses it later; prefer operating on a single file handle; inferred tags: toctou_safe) but no covering requirement closes obligation(s): filesystem_operations_use_handle. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-003 -> src/grep.rs has signal "error_discarded" (discards result via let _ = <call>; inferred tags: error_propagation) but no covering requirement closes obligation(s): errors_propagated. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-003 -> src/grep.rs has signal "filesystem_dependency" (uses filesystem via std::fs::File::open; inferred tags: fs_io) but no covering requirement closes obligation(s): external_call_concurrency_bounded, external_call_failure_observable, external_call_timeout_bounded, upstream_budget_coordinated. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-003 -> src/grep.rs has signal "panic_risk" (uses unreachable! macro; inferred tags: panic_safety) but no covering requirement closes obligation(s): panic_free_input_handling. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-005 -> src/language/tree_cache.rs has signal "panic_risk" (calls .unwrap() (panics on Err/None); inferred tags: panic_safety) but no covering requirement closes obligation(s): panic_free_input_handling. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-006 -> src/lsp_integration/client.rs has signal "error_discarded" (discards error via .ok() on Result; inferred tags: error_propagation) but no covering requirement closes obligation(s): errors_propagated. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-006 -> src/lsp_integration/client.rs has signal "filesystem_dependency" (uses filesystem via tokio::fs::read_dir; inferred tags: fs_io) but no covering requirement closes obligation(s): external_call_concurrency_bounded, external_call_failure_observable, external_call_timeout_bounded, upstream_budget_coordinated. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-006 -> src/lsp_integration/client.rs has signal "lossy_string_conversion" (calls .to_string_lossy() (drops invalid bytes); inferred tags: binary_safe_io) but no covering requirement closes obligation(s): binary_data_preserved. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-006 -> src/lsp_integration/client.rs has signal "panic_risk" (uses slice indexing (panics on out-of-bounds); inferred tags: panic_safety) but no covering requirement closes obligation(s): panic_free_input_handling. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-006 -> src/lsp_integration/management.rs has signal "error_discarded" (discards result via let _ = <call>; inferred tags: error_propagation) but no covering requirement closes obligation(s): errors_propagated. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-006 -> src/lsp_integration/management.rs has signal "filesystem_dependency" (uses filesystem via std::fs::remove_file; inferred tags: fs_io) but no covering requirement closes obligation(s): external_call_concurrency_bounded, external_call_failure_observable, external_call_timeout_bounded, upstream_budget_coordinated. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-006 -> src/lsp_integration/management.rs has signal "lossy_string_conversion" (calls .to_string_lossy() (drops invalid bytes); inferred tags: binary_safe_io) but no covering requirement closes obligation(s): binary_data_preserved. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-006 -> src/lsp_integration/management.rs has signal "panic_risk" (uses slice indexing (panics on out-of-bounds); inferred tags: panic_safety) but no covering requirement closes obligation(s): panic_free_input_handling. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-006 -> src/lsp_integration/management.rs has signal "toctou_pair" (checks path via std::fs::remove_file then uses it later; prefer operating on a single file handle; inferred tags: toctou_safe) but no covering requirement closes obligation(s): filesystem_operations_use_handle. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       src/main.rs src/main.rs:325 has signal "error_discarded" (discards error via .ok() on Result; inferred tags: error_propagation) but 2 linked requirements share the artifact and no covering requirement closes obligation(s): errors_propagated; narrow implemented_by ownership or add a covering requirement with obligation_class, implemented_by, and verified_by evidence for the true owner. Owners: SYS-REQ-006, SYS-REQ-007
       src/main.rs src/main.rs:1102 has signal "filesystem_dependency" (uses filesystem via std::fs::read_to_string; inferred tags: fs_io) but 2 linked requirements share the artifact and no covering requirement closes obligation(s): external_call_concurrency_bounded, external_call_failure_observable, external_call_timeout_bounded, upstream_budget_coordinated; narrow implemented_by ownership or add a covering requirement with obligation_class, implemented_by, and verified_by evidence for the true owner. Owners: SYS-REQ-006, SYS-REQ-007
       src/main.rs src/main.rs:451 has signal "lossy_string_conversion" (calls .to_string_lossy() (drops invalid bytes); inferred tags: binary_safe_io) but 2 linked requirements share the artifact and no covering requirement closes obligation(s): binary_data_preserved; narrow implemented_by ownership or add a covering requirement with obligation_class, implemented_by, and verified_by evidence for the true owner. Owners: SYS-REQ-006, SYS-REQ-007
       src/main.rs src/main.rs:197 has signal "panic_risk" (calls .unwrap() (panics on Err/None); inferred tags: panic_safety) but 2 linked requirements share the artifact and no covering requirement closes obligation(s): panic_free_input_handling; narrow implemented_by ownership or add a covering requirement with obligation_class, implemented_by, and verified_by evidence for the true owner. Owners: SYS-REQ-006, SYS-REQ-007
       src/main.rs src/main.rs:1097 has signal "toctou_pair" (checks path via .exists then uses it later; prefer operating on a single file handle; inferred tags: toctou_safe) but 2 linked requirements share the artifact and no covering requirement closes obligation(s): filesystem_operations_use_handle; narrow implemented_by ownership or add a covering requirement with obligation_class, implemented_by, and verified_by evidence for the true owner. Owners: SYS-REQ-006, SYS-REQ-007
       SYS-REQ-004 -> src/path_resolver/go.rs has signal "lossy_string_conversion" (uses String::from_utf8_lossy (drops invalid UTF-8); inferred tags: binary_safe_io) but no covering requirement closes obligation(s): binary_data_preserved. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-004 -> src/path_resolver/go.rs has signal "panic_risk" (uses slice indexing (panics on out-of-bounds); inferred tags: panic_safety) but no covering requirement closes obligation(s): panic_free_input_handling. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-004 -> src/path_resolver/javascript.rs has signal "filesystem_dependency" (uses filesystem via std::fs::write; inferred tags: fs_io) but no covering requirement closes obligation(s): external_call_concurrency_bounded, external_call_failure_observable, external_call_timeout_bounded, upstream_budget_coordinated. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-004 -> src/path_resolver/javascript.rs has signal "lossy_string_conversion" (uses String::from_utf8_lossy (drops invalid UTF-8); inferred tags: binary_safe_io) but no covering requirement closes obligation(s): binary_data_preserved. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-004 -> src/path_resolver/javascript.rs has signal "panic_risk" (uses slice indexing (panics on out-of-bounds); inferred tags: panic_safety) but no covering requirement closes obligation(s): panic_free_input_handling. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-004 -> src/path_resolver/rust.rs has signal "filesystem_dependency" (uses filesystem via std::fs::read_dir; inferred tags: fs_io) but no covering requirement closes obligation(s): external_call_concurrency_bounded, external_call_failure_observable, external_call_timeout_bounded, upstream_budget_coordinated. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-004 -> src/path_resolver/rust.rs has signal "lossy_string_conversion" (calls .to_string_lossy() (drops invalid bytes); inferred tags: binary_safe_io) but no covering requirement closes obligation(s): binary_data_preserved. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-004 -> src/path_resolver/rust.rs has signal "panic_risk" (uses slice indexing (panics on out-of-bounds); inferred tags: panic_safety) but no covering requirement closes obligation(s): panic_free_input_handling. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-003 -> src/path_safety.rs has signal "error_discarded" (discards error via .ok() on Result; inferred tags: error_propagation) but no covering requirement closes obligation(s): errors_propagated. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       SYS-REQ-003 -> src/path_safety.rs has signal "filesystem_dependency" (uses filesystem via std::fs::symlink_metadata; inferred tags: fs_io) but no covering requirement closes obligation(s): external_call_concurrency_bounded, external_call_failure_observable, external_call_timeout_bounded, upstream_budget_coordinated. Add a covering requirement with obligation_class, implemented_by for this artifact, and verified_by evidence, or suppress with rationale if the signal is intentionally outside the contract.
       src/ranking.rs src/ranking.rs:374 has signal "panic_risk" (uses slice indexing (panics on out-of-bounds); inferred tags: panic_safety) but 2 linked requirements share the artifact and no covering requirement closes obligation(s): panic_free_input_handling; narrow implemented_by ownership or add a covering requirement with obligation_class, implemented_by, and verified_by evidence for the true owner. Owners: SYS-REQ-001, SYS-REQ-007

       WHAT: 49 code signal obligation ownership finding(s)
       WHY:  Implementation code can reveal runtime and boundary behavior that the requirement never described: randomness, goroutines, channels, map-order sensitivity, HTTP clients, filesystem access, subprocesses, and project-specific internal APIs. The check turns direct signal obligations and catalog obligations derived from inferred tags into grouped findings, so shared code with many requirement annotations does not fan out into one warning per linked requirement.
       HOW:  Inspect the grouped artifact first:
       proof catalog suggest --from-code --req <REQ-ID>
Then add or identify a covering requirement that explicitly closes the scenario:
       proof req new system --component <component> --obligation-class <obligation-id> --description "..."
       proof req link add <NEW-REQ-ID> implemented_by <artifact-or-symbol>
       proof req link add <NEW-REQ-ID> verified_by <test-or-evidence>
       proof req link add <NEW-REQ-ID> satisfies <OWNER-OR-PARENT-REQ-ID>   # if this requirement decomposes another requirement
Do not use --parent as loose grouping; use it only when you intentionally model a hierarchy. Use --add-obligation on a parent only to declare/decompose an obligation; it does not close this check by itself. Suppress only when the signal is intentionally outside the contract:
       proof req edit <REQ-ID> --suppress-obligation <obligation-id> --reason "..."
If many requirements share the same file, narrow implemented_by to the true owning function or requirement before accepting the obligation.
       DOCS: docs/help/checks/code_signal_obligations_reviewed.md
       Help: proof help code_signal_obligations_reviewed


  BLOCKED: 1 warning(s) must be resolved before advancing under fail-level warn
  Resolve the warning(s) above, then run: proof workflow check

  Run: proof workflow check   (after fixing issues)
  Help: proof help workflow
  Help: proof help <check-id>  # replace <check-id> with any check listed above

